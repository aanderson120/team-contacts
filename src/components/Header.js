import React from "react";
import "../index.css";

function Header () {
    return (
        <header>
            <h1 id = "header">Team Directory</h1>
            <p id="headerBody">Search for an employee by typing in their info or clicking on a column to sort.</p>
        </header>
    );
}

export default Header